node_modules
.next
.git
.github
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.local
*.log
tsconfig.tsbuildinfo

# 本地环境变量：builder 阶段是 `COPY . .`，不排除的话本地 .env 会进入镜像层，
# next build 还会把里面的 NEXT_PUBLIC_* 直接内联进客户端产物
.env
.env.*
!.env.example

# 本地 SQLite（.env.example 里的 DATABASE_URL 默认就是 file:./dev.db）
*.db
*.db-journal

# 本地开发目录，不需要进构建上下文
/examples/
/skills/
/download/
/agent-ctx/
.vercel
.idea/
.vscode/
